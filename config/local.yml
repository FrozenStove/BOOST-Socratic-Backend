# Local development configuration (for running backend locally, databases in Docker)
chromaDbUrl: "http://localhost:8000"
database:
  url: "postgresql://postgres:postgres@localhost:5432/postgres"
port: 3010

